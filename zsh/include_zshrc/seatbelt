# Thin shim: seatbelt() lives in aifiles skills/seatbelt (bash+zsh).
# Source only if the skill file is present.
() {
  local f="${AIFILES:-$HOME/aifiles}/skills/seatbelt/seatbelt.sh"
  [[ -r $f ]] && source $f
}
